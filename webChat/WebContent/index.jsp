<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<!doctype html>
<html>
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
<title>跳转中...</title>
</head>
<body>
<%-- 	<c:choose>
		<c:when test="${header['code'] == null || header['code'] == 1}">
			<jsp:forward page="index.do"></jsp:forward>
		</c:when>
		<c:when test="${header['code'] != null && header['code'] == 2}">
			<jsp:forward page="cms/login.do"></jsp:forward>
		</c:when>
		<c:when test="${header['code'] != null && header['code'] == 3}">
			<jsp:forward page="toMall.do"></jsp:forward>
		</c:when>
		<c:otherwise>
			<jsp:forward page="index.do"></jsp:forward>
		</c:otherwise>
	</c:choose> --%>
</body>
</html>